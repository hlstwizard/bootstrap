export MCP_ENDPOINT_HOST="127.0.0.1"

# Optional override when syncing OpenCode MCP URLs:
# export OPENCODE_MCP_ENDPOINT_HOST="127.0.0.1"

# Optional custom MCP server config path:
# export MCP_SERVERS_FILE="$HOME/bootstrap/mcp/servers.conf"

# Secret values should be managed via Bitwarden (~/.bw-env), not plaintext here.
# Example in ~/.bw-env:
# CONTEXT7_API_KEY|context7-api-key|password
